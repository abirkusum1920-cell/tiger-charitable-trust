const FF='"Hind Siliguri","Noto Sans Bengali",sans-serif';let fontsReady=null;function ensureFonts(){return fontsReady||=Promise.all(["400","600","700"].map(w=>document.fonts.load(`${w} 40px "Hind Siliguri"`,"বাংলা"))).catch(()=>{})}function fnt(x,w,px){x.font=`${w} ${px}px ${FF}`}function rrect(x,a,b,w,h,r){x.beginPath();x.moveTo(a+r,b);x.arcTo(a+w,b,a+w,b+h,r);x.arcTo(a+w,b+h,a,b+h,r);x.arcTo(a,b+h,a,b,r);x.arcTo(a,b,a+w,b,r);x.closePath()}function wrapT(x,t,a,b,maxW,lh,lines=2){const ws=String(t||"").split(/\s+/);let line="",out=[];for(const w of ws){const tt=line?line+" "+w:w;if(x.measureText(tt).width>maxW&&line){out.push(line);line=w}else line=tt}if(line)out.push(line);if(out.length>lines){out=out.slice(0,lines);out[lines-1]+="…"}out.forEach((l,i)=>x.fillText(l,a,b+i*lh));return out.length*lh}function fitT(x,t,w0,px,maxW){let p=px;do{fnt(x,w0,p);p-=2}while(x.measureText(t).width>maxW&&p>20)}async function drawLogo(x,cx,cy,r){x.save();x.beginPath();x.arc(cx,cy,r,0,Math.PI*2);x.fillStyle="#fff";x.fill();x.clip();const img=S.logo?await loadImg(S.logo):null;if(img)x.drawImage(img,cx-r,cy-r,r*2,r*2);else{x.fillStyle="#C2410C";fnt(x,700,r*.52);x.textAlign="center";x.textBaseline="middle";x.fillText("TIGER",cx,cy+2)}x.restore();x.textAlign="left";x.textBaseline="alphabetic"}async function drawPhoto(x,id,a,b,w,h,r){const src=await photoOf(id)||AVATAR;const img=await loadImg(src);x.save();rrect(x,a,b,w,h,r);x.clip();x.fillStyle="#F1E7DB";x.fillRect(a,b,w,h);if(img)x.drawImage(img,a,b,w,h);x.restore();x.lineWidth=6;x.strokeStyle="#EA7A1E";rrect(x,a,b,w,h,r);x.stroke()}function header(x,W,h,sub){const g=x.createLinearGradient(0,0,W,h);g.addColorStop(0,"#B8390A");g.addColorStop(1,"#EA7A1E");x.fillStyle=g;x.fillRect(0,0,W,h);x.fillStyle="#fff";fnt(x,700,66);x.fillText("T I G E R",230,h*.44);fnt(x,500,38);x.fillText("The Charitable Trust",232,h*.44+50);fnt(x,500,29);x.globalAlpha=.92;x.fillText(sub,232,h*.44+92);x.globalAlpha=1}const canvasFile=(c,name)=>new Promise(res=>c.toBlob(b=>res(new File([b],name,{type:"image/jpeg"})),"image/jpeg",.9));async function drawSlip(s){await ensureFonts();const m=S.members.get(s.memberId),W=1080,H=1400;const c=document.createElement("canvas");c.width=W;c.height=H;const x=c.getContext("2d");x.fillStyle="#FFF9F0";x.fillRect(0,0,W,H);header(x,W,250,"যোগাযোগ: "+bn(CONTACT));await drawLogo(x,125,125,82);x.textAlign="center";x.fillStyle="#7C2D12";fnt(x,700,54);x.fillText("সেবা বিতরণ স্লিপ",W/2,335);x.strokeStyle="#EA7A1E";x.lineWidth=3;x.beginPath();x.moveTo(330,355);x.lineTo(750,355);x.stroke();x.fillStyle="#FFE8CC";rrect(x,60,385,W-120,190,26);x.fill();x.strokeStyle="#EA7A1E";x.lineWidth=4;x.stroke();x.fillStyle="#9A3412";fnt(x,500,34);x.fillText("সামগ্রী",W/2,438);x.fillStyle="#C2410C";fitT(x,s.item,700,104,W-180);x.fillText(s.item,W/2,540);x.textAlign="left";await drawPhoto(x,m.id,70,625,300,340,26);x.fillStyle="#1F1A17";fnt(x,700,58);let y=690+wrapT(x,m.name,410,690,610,64,2)-64;y+=68;fnt(x,600,40);x.fillStyle="#C2410C";x.fillText("সদস্য নং: "+(m.regNo?bn(m.regNo):"—"),410,y);x.fillStyle="#4A413B";fnt(x,500,35);const rows=[[m.gender==="F"?"মা":"বাবা",m.age?bn(m.age)+" বছর":""].filter(Boolean).join(" · "),m.guardian?"পিতা/স্বামী: "+m.guardian:"",m.village?"গ্রাম: "+m.village:"",m.phone?"ফোন: "+bn(m.phone):""].filter(Boolean);for(const r of rows){y+=54;y+=wrapT(x,r,410,y,610,44,1)-44}x.setLineDash([14,10]);x.strokeStyle="#D6C3AE";x.lineWidth=3;x.beginPath();x.moveTo(60,1015);x.lineTo(W-60,1015);x.stroke();x.setLineDash([]);x.fillStyle="#1F1A17";fnt(x,700,40);x.fillText("স্লিপ নং: "+s.code,70,1085);x.textAlign="right";fnt(x,600,38);x.fillText("তারিখ: "+dstr(s.createdAt),W-70,1085);x.textAlign="left";x.fillStyle="#F3EADF";rrect(x,60,1120,W-120,110,18);x.fill();x.fillStyle="#5B4E45";fnt(x,500,31);wrapT(x,"এই স্লিপটি সঙ্গে নিয়ে এসে সামগ্রী সংগ্রহ করুন। একজনের নামে একটি সামগ্রীর একটিই স্লিপ বৈধ।",90,1165,W-180,42,2);x.strokeStyle="#8A7B70";x.lineWidth=2;x.beginPath();x.moveTo(W-420,1320);x.lineTo(W-70,1320);x.stroke();x.fillStyle="#6B625B";fnt(x,500,28);x.textAlign="center";x.fillText("কর্তৃপক্ষের স্বাক্ষর",W-245,1358);x.textAlign="left";fnt(x,600,28);x.fillStyle="#C2410C";x.fillText("T I G E R The Charitable Trust",70,1358);x.strokeStyle="#EA7A1E";x.lineWidth=10;x.strokeRect(5,5,W-10,H-10);return canvasFile(c,`slip-${s.code}.jpg`)}async function drawCard(m){await ensureFonts();const W=1200,H=760;const c=document.createElement("canvas");c.width=W;c.height=H;const x=c.getContext("2d");x.fillStyle="#FFFDF9";x.fillRect(0,0,W,H);x.globalAlpha=.06;x.fillStyle="#EA7A1E";x.beginPath();x.arc(W-120,H-150,260,0,7);x.fill();x.globalAlpha=1;header(x,W,200,"সদস্য পরিচয়পত্র");await drawLogo(x,115,100,76);x.fillStyle="#fff";rrect(x,W-250,52,200,96,18);x.fill();x.fillStyle="#C2410C";x.textAlign="center";fnt(x,500,26);x.fillText("সদস্য নং",W-150,88);fnt(x,700,44);x.fillText(m.regNo?bn(m.regNo):"—",W-150,134);x.textAlign="left";await drawPhoto(x,m.id,50,235,280,330,22);x.fillStyle="#1F1A17";fnt(x,700,54);let y=290;y+=wrapT(x,m.name,370,y,780,60,2)-60;const kv=[["পিতা/স্বামী",m.guardian],["বয়স",[m.age?bn(m.age)+" বছর":"",m.gender==="F"?"মহিলা":"পুরুষ"].filter(Boolean).join(" · ")],["গ্রাম",m.village],["ঠিকানা",m.address],["ফোন",m.phone?bn(m.phone):""],["আধার",m.aadhaarLast4?"XXXX XXXX "+bn(m.aadhaarLast4):""]].filter(r=>r[1]);for(const[k,v]of kv){y+=56;fnt(x,500,30);x.fillStyle="#8A7B70";x.fillText(k,370,y);fnt(x,600,32);x.fillStyle="#2B2420";y+=wrapT(x,v,530,y,620,40,k==="ঠিকানা"?2:1)-40;if(y>600)break}x.fillStyle="#166534";x.fillRect(0,H-80,W,80);x.fillStyle="#fff";fnt(x,500,29);x.fillText("নথিভুক্তি: "+dstr(m.createdAt),50,H-30);x.textAlign="right";x.fillText("যোগাযোগ: "+bn(CONTACT),W-50,H-30);x.textAlign="left";return canvasFile(c,`member-${m.regNo||m.id}.jpg`)}async function sendFiles(files,text){if(navigator.canShare&&navigator.canShare({files})){try{await navigator.share({files,text});return}catch(e){if(e.name==="AbortError")return}}files.forEach(f=>{const a=document.createElement("a");a.href=URL.createObjectURL(f);a.download=f.name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),4e3)});toast("ছবি ডাউনলোড হল — WhatsApp চ্যাটে সংযুক্ত করুন",4e3);openChat(text)}function openChat(text){const n=waNum();if(!n)return toast("সেটিংসে WhatsApp নম্বর দিন");window.open(`https://wa.me/${n}?text=${encodeURIComponent(text||"")}`,"_blank")}function shareSheet(title,jobs,textFn){const size=20,groups=[];for(let i=0;i<jobs.length;i+=size)groups.push(jobs.slice(i,i+size));S.shareGroups=groups.map(g=>({jobs:g,files:null}));S.shareText=textFn;const n=waNum();openModal(`<div class="shead"><h3>${title}</h3><button class="x" onclick="closeModal()">×</button></div>
    <div class="note ok">পাঠান চাপলে WhatsApp খুলবে — সেখানে ${n?`<b>${bn(S.cfg.wa)}</b>`:"আপনার নম্বর"} চ্যাটটি বেছে নিন। (নম্বরটা WhatsApp-এ পিন করে রাখলে এক ছোঁয়ায় পাবেন)</div>
    <img id="shPrev" class="prev" style="display:none" alt="">
    ${S.shareGroups.map((g,i)=>`<div class="chunk"><span>${groups.length>1?`${bn(i*size+1)}–${bn(i*size+g.jobs.length)} নং`:`${bn(g.jobs.length)}টি ছবি`}</span>
      <button class="btn wa" id="shB${i}" onclick="shareGroup(${i})">${IC.wa} তৈরি হচ্ছে…</button></div>`).join("")}
    <div class="btns">${n?`<button class="btn full" onclick="openChat(S.shareText(0))">${IC.wa} শুধু ${bn(S.cfg.wa)} চ্যাট খুলুন</button>`:'<button class="btn full" onclick="openSettings()">WhatsApp নম্বর সেট করুন</button>'}</div>`);prepGroup(0)}async function prepGroup(i){const g=S.shareGroups[i];if(!g||g.files||g.busy)return;g.busy=true;const b=$("#shB"+i);if(!b)return;b.disabled=true;g.files=[];for(let k=0;k<g.jobs.length;k++){b.innerHTML=`তৈরি হচ্ছে ${bn(k+1)}/${bn(g.jobs.length)}`;g.files.push(await g.jobs[k]())}if(i===0&&g.files[0]){const p=$("#shPrev");if(p){p.src=URL.createObjectURL(g.files[0]);p.style.display="block"}}b.disabled=false;b.innerHTML=`${IC.wa} পাঠান`;g.busy=false}async function shareGroup(i){const g=S.shareGroups[i];if(!g.files){return prepGroup(i)}await sendFiles(g.files,S.shareText(i));const b=$("#shB"+i);if(b)b.innerHTML=`${IC.wa} আবার পাঠান`;const nx=$("#shB"+(i+1));if(nx&&!S.shareGroups[i+1].files){prepGroup(i+1)}}function shareSlips(list,title){list=list.filter(Boolean);if(!list.length)return;const groups=[];shareSheet(title||(list.length>1?`${bn(list.length)}টি স্লিপ পাঠান`:"স্লিপ পাঠান"),list.map(s=>()=>drawSlip(s)),i=>list.slice(i*20,i*20+20).map(s=>{const m=S.members.get(s.memberId);return`${m.name} (নং ${bn(m.regNo||"-")}) — ${s.item} — ${s.code}`}).join("\n"))}async function shareCard(id){const m=S.members.get(id);if(!m.regNo&&navigator.onLine){toast("সদস্য নং পেতে সিঙ্ক হচ্ছে…");await syncNow()}shareSheet("সদস্যপত্র পাঠান",[()=>drawCard(S.members.get(id))],()=>`সদস্যপত্র: ${m.name} (নং ${bn(S.members.get(id).regNo||"-")})`)}function openSettings(first){const c=S.cfg;openModal(`<div class="shead"><h3>সেটিংস</h3>${first?"":'<button class="x" onclick="closeModal()">×</button>'}</div>
    ${first?'<div class="note warn">প্রথমবার: পাসকোড, WhatsApp নম্বর আর Gemini key দিন</div>':""}
    <div class="field"><label>Apps Script Web App লিংক (/exec)</label><input id="s_url" value="${esc(c.url||"")}" placeholder="https://script.google.com/macros/s/…/exec"></div>
    <div class="field"><label>পাসকোড</label><input id="s_key" type="password" value="${esc(c.key||"")}"></div>
    <div class="field"><label>যে WhatsApp নম্বরে স্লিপ ও সদস্যপত্র যাবে</label><input id="s_wa" inputmode="tel" value="${esc(c.wa||"")}" placeholder="১০ সংখ্যার নম্বর">
      <div class="hint">এটা সার্ভারেও জমা থাকে, তাই অন্য ফোনেও একই নম্বর পাবেন।</div></div>
    <div class="field"><label>Gemini API key (আধার পড়ার জন্য) ${S.hasKey?"— ✓ দেওয়া আছে":""}</label><input id="s_gk" type="password" placeholder="${S.hasKey?"বদলাতে চাইলে নতুন key দিন":"এখানে key পেস্ট করুন"}">
      <div class="hint">key সরাসরি আপনার Google Apps Script-এ জমা হয়, এই ফোনে থাকে না।</div></div>
    <div class="phbox"><img src="${S.logo||AVATAR}" id="s_logoPrev" style="width:64px;height:64px;border-radius:50%" alt="">
      <div class="col"><button class="btn" onclick="pick('#fLogo')">ট্রাস্টের লোগো (স্লিপে দেখাবে)</button></div></div>
    <div class="btns">
      <button class="btn pri full" onclick="saveSettings()">সংরক্ষণ ও সংযোগ পরীক্ষা</button>
      <button class="btn soft full" onclick="openBulk()">👥 একসাথে অনেক সদস্য যোগ</button>
      <button class="btn" onclick="exportCSV()">${IC.dl} Excel (CSV)</button>
      <button class="btn" onclick="fullReload()">সব নতুন করে আনুন</button>
    </div>
    <div class="hint" style="margin-top:12px">ফোনে জমা: ${bn(S.members.size)} সদস্য · ${bn(S.slips.size)} স্লিপ · পাঠানো বাকি ${bn(S.ob)}টি কাজ</div>`)}async function saveSettings(){const wa=en($("#s_wa").value).replace(/\D/g,"");const waChanged=wa!==(S.cfg.wa||"");S.cfg={url:$("#s_url").value.trim(),key:$("#s_key").value.trim(),wa};saveCfg();if(!S.cfg.url)S.cfg.url=DEFAULT_URL;if(!S.cfg.key)return toast("পাসকোড লিখুন");try{toast("সংযোগ পরীক্ষা হচ্ছে…",1e4);const pg=await api("ping");S.hasKey=pg.hasKey;const gk=$("#s_gk").value.trim();if(waChanged||gk)await api("settings",{waNumber:wa,geminiKey:gk||void 0});if(gk)S.hasKey=true;toast("সংযোগ ঠিক আছে ✓");closeModal();syncNow()}catch(e){toast(e.message==="AUTH"?"পাসকোড ভুল":"সংযোগ হল না: "+e.message,4e3)}}async function fullReload(){if(!confirm("সার্ভার থেকে সব তথ্য আবার আনা হবে (একটু সময় লাগবে)। চালাবেন?"))return;await setMeta("since",0);S.ppv={};await setMeta("ppv",{});closeModal();syncNow(true)}function exportCSV(){const q=v=>'"'+String(v??"").replace(/"/g,'""')+'"';const head=["সদস্য নং","নাম","ইংরেজি নাম","মা/বাবা","বয়স","পিতা/স্বামী","ফোন","গ্রাম","ঠিকানা","আধার শেষ ৪","প্রাপ্ত সামগ্রী","নথিভুক্তি"];const rows=actMembers().sort((a2,b)=>a2.regNo-b.regNo).map(m=>[m.regNo,m.name,m.nameEn,GL[m.gender],m.age||"",m.guardian,m.phone,m.village,m.address,m.aadhaarLast4,(S.byMember.get(m.id)||[]).map(s=>s.item).join(", "),new Date(m.createdAt).toLocaleDateString("en-GB")]);const csv="\uFEFF"+[head,...rows].map(r=>r.map(q).join(",")).join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));a.download=`tiger-sadasya-${new Date().toISOString().slice(0,10)}.csv`;a.click()}function openBulk(){openModal(`<div class="shead"><h3>একসাথে অনেক সদস্য যোগ</h3><button class="x" onclick="closeModal()">×</button></div>
    <div class="note ok">প্রতি লাইনে একজন: <b>নাম, ফোন, মা/বাবা, গ্রাম</b><br>বয়স বা পিতা/স্বামীর নামও দেওয়া যায়।</div>
    <div class="field"><textarea id="bulkTxt" rows="10" placeholder="প্রতিমা দাস, 9876543210, মা, সিউড়ি&#10;হরিপদ মণ্ডল, 9123456789, বাবা, সাঁইথিয়া"></textarea></div>
    <div id="bulkMsg"></div>
    <div class="btns"><button class="btn" onclick="checkBulk()">আগে দেখে নিন</button><button class="btn pri" id="bulkGo" onclick="runBulk()">যোগ করুন</button></div>`)}function parseBulk(txt){const rows=[],bad=[];txt.split(/\r?\n/).forEach((line,i)=>{const raw=line.trim();if(!raw)return;const parts=raw.split(/\s*[,|\t;]\s*/).map(x=>x.trim()).filter(Boolean);const r={name:"",phone:"",gender:"",village:"",age:0,guardian:""},rest=[];for(const p0 of parts){const p=en(p0),d=p.replace(/[\s-]/g,"");if(!r.phone&&/^(\+?91)?[6-9]\d{9}$/.test(d))r.phone=d.slice(-10);else if(!r.gender&&/^(মা|মহিলা|নারী|স্ত্রী|f|female|w|woman|mother|ma)$/i.test(p))r.gender="F";else if(!r.gender&&/^(বাবা|পুরুষ|m|male|man|father|baba)$/i.test(p))r.gender="M";else if(!r.age&&/^\d{1,3}$/.test(d)&&+d>0&&+d<120)r.age=+d;else rest.push(p0)}r.name=rest[0]||"";r.village=rest[1]||"";r.guardian=rest[2]||"";if(!r.name||!r.gender)bad.push({n:i+1,line:raw,why:!r.name?"নাম নেই":"মা/বাবা লেখা নেই"});else rows.push(r)});const have=new Set(actMembers().filter(m=>m.phone).map(m=>norm(m.name)+"|"+m.phone)),seen=new Set,add=[],dup=[];rows.forEach(r=>{const k=norm(r.name)+"|"+r.phone;if(r.phone&&(have.has(k)||seen.has(k)))dup.push(r);else{add.push(r);if(r.phone)seen.add(k)}});return{add,dup,bad}}function bulkReport(p){const f=p.add.filter(r=>r.gender==="F").length;return`<div class="note ok">যোগ হবে: <b>${bn(p.add.length)} জন</b> (মা ${bn(f)}, বাবা ${bn(p.add.length-f)})</div>`+(p.dup.length?`<div class="note warn">আগেই আছেন, তাই বাদ: ${bn(p.dup.length)} জন — ${p.dup.slice(0,5).map(r=>esc(r.name)).join(", ")}${p.dup.length>5?"…":""}</div>`:"")+(p.bad.length?`<div class="note bad">এই লাইনগুলো ঠিক পড়া যায়নি (বাদ যাবে):<br>${p.bad.slice(0,8).map(b=>`লাইন ${bn(b.n)}: ${esc(b.line)} — ${b.why}`).join("<br>")}${p.bad.length>8?"<br>…":""}</div>`:"")}function checkBulk(){$("#bulkMsg").innerHTML=bulkReport(parseBulk($("#bulkTxt").value))}async function runBulk(){const p=parseBulk($("#bulkTxt").value);$("#bulkMsg").innerHTML=bulkReport(p);if(!p.add.length)return toast("যোগ করার মতো কেউ নেই");const b=$("#bulkGo");b.disabled=true;const t0=Date.now(),recs=[];p.add.forEach((r,i)=>{const id=uid("m");recs.push({id,regNo:0,name:r.name,nameEn:"",gender:r.gender,age:r.age,guardian:r.guardian,phone:r.phone,village:r.village,address:"",note:"",aadhaarLast4:"",pv:0,createdAt:t0+i,updatedAt:t0+i,deleted:0})});await DB.putMany("members",recs);recs.forEach(m=>S.members.set(m.id,m));await new Promise((res,rej)=>{const t=DB.db.transaction("outbox","readwrite"),o=t.objectStore("outbox");recs.forEach(m=>o.add({op:{op:"saveMember",id:m.id,data:{id:m.id,name:m.name,nameEn:"",gender:m.gender,age:m.age,guardian:m.guardian,phone:m.phone,village:m.village,address:"",note:""}}}));t.oncomplete=res;t.onerror=()=>rej(t.error)});await refreshPending();render();closeModal();toast(`${bn(recs.length)} জন যোগ হল ✓ — সার্ভারে পাঠানো চলছে, সাইট খোলা রাখুন`,5e3);syncNow()}$("#fScanFront").onchange=e=>onScanForm(e.target.files[0]);$("#fScanSearch").onchange=e=>onScanSearch(e.target.files[0]);$("#fPhotoCam").onchange=e=>onPhoto(e.target.files[0]);$("#fPhotoGal").onchange=e=>onPhoto(e.target.files[0]);$("#fLogo").onchange=async e=>{const f=e.target.files[0];if(!f)return;S.logo=await compress(f,300,.85,true);await setMeta("logo",S.logo);$("#s_logoPrev").src=S.logo;setLogo();toast("লোগো সেট হল")};function setLogo(){if(S.logo)$("#hLogo").innerHTML=`<img src="${S.logo}" alt="">`}let qt;$("#q").addEventListener("input",e=>{clearTimeout(qt);qt=setTimeout(()=>{S.q=e.target.value;renderList(true)},150)});new IntersectionObserver(es=>{if(es[0].isIntersecting)moreRows()},{rootMargin:"400px"}).observe($("#sentinel"));addEventListener("online",()=>syncNow());addEventListener("offline",status);document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible")syncNow()});(async function boot(){loadCfg();await DB.open();if(!S.cfg.key){const c=await getMeta("cfg");if(c&&c.key)S.cfg=c}if(!S.cfg.url)S.cfg.url=DEFAULT_URL;const[ms,ss,ppv,logo]=await Promise.all([DB.all("members"),DB.all("slips"),getMeta("ppv"),getMeta("logo")]);ms.forEach(m=>S.members.set(m.id,m));ss.forEach(s=>S.slips.set(s.id,s));S.ppv=ppv||{};S.logo=logo||"";setLogo();await refreshPending();render();if(!S.cfg.url||!S.cfg.key)openSettings(true);else syncNow();setInterval(()=>{if(document.visibilityState==="visible")syncNow()},9e4);if("serviceWorker"in navigator)navigator.serviceWorker.register("sw.js").catch(()=>{})})();
    
